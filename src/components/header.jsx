import React from "react";

const Header = () => {
    return (
        <div className="" style={{display: "flex", justifyContent: "center"}}>
            Fono: | Ventas: 
        </div>

    );
};

export default Header;